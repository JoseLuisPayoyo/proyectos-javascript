const producto = 'Monitor de 20 pulgadas';

//.repeat permite repetir cadena de texto
const texto = 'en promocion'.repeat(3);
console.log(texto);

//split, dividir string
const actividad = "Estoy aprendiendo JS Moderno";
console.log(actividad.split(" "))